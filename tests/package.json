{
  "name": "hotel-booker-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Hotel Booker App",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:api": "playwright test api-tests",
    "test:frontend": "playwright test frontend-tests"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "dotenv": "^16.3.1"
  }
}